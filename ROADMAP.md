# ROADMAP

`/loop` が毎サイクル 1 項目を拾うためのバックログ。**着手可能** から S サイズを 1 つ選び、
実装 → テスト → 自己レビュー → ドキュメント更新 → PR の 1 サイクルを回す。

- **撤退条件は「同じ失敗を繰り返したら止める」。** テストが 2 回続けて落ちても、
  **失敗の数が減っている / 原因が前より絞れている**なら続けてよい（2026-08-31 決定）。
  止めるのは、同じ失敗を同じ理解のまま繰り返しているとき。
- **空振りが 2 回続いたらループを止める**（2026-08-31 決定）。着手できる項目が無い状態で
  発火し続けても、同じ報告を繰り返すだけになる。判断待ちが解けたら打ち直す。
- 判断が要ることはチャットで聞かず **Issue に上げる**（`question` ラベル）。
  **ループは非同期なので、チャットに書いた質問は誰にも届かない。** 2026-08-31 に、
  数サイクル続けてチャットで聞いてしまっていたのを直した（→ [#81](https://github.com/ken-ty/ccs/issues/81)、
  [#82](https://github.com/ken-ty/ccs/issues/82)）。**着手できない項目は、依存の欄に Issue 番号を書く。**
- 完了した項目はこの表から消し、[完了ログ](#完了ログ) に 1 行で残す。
- **設計の正本は [docs/design.md](docs/design.md)。** 迷ったらチャットではなくそちらを読む。

## 確定している設計方針

毎サイクル読み直す前提。これに反する実装をしないこと。

| 方針 | 決定 | 出所 |
| --- | --- | --- |
| 自作する範囲 | **4 つだけ** — 新規作成 / trust 回避 / パス解決 / 命名規約と後片付け | design.md §1 |
| 一覧・状態 | `claude agents --json` に委譲。**自前のデーモンやポーリングを作らない** | design.md §2.1 |
| セッション間通信 | 組み込みの `ListAgents` / `SendMessage`。**`tmux send-keys` でのプロンプト注入は採らない** | design.md §3 |
| 命名 | tmux は `cc/<slug>`、Claude の `-n` は `<slug>`、id は `--session-id` で固定。**slug に `@` は使わない**（`SendMessage` の `name@team` と衝突。worktree の区切りは `--`）。**ただし名前は人が変える。同一性の根拠にしない**（→ 同一性の行） | design.md §4.2 |
| 同一性 | 場所は `ccs` が発行する **`workspaceId`**（ディレクトリ名そのもの）、会話は Claude の **`sessionId`**。**tmux 名も claude の `name` も表示用のラベル**で、占有・冪等性・素性の判定に使わない。**発行したディレクトリには印（`.ccs.json`）を刻む** | [ADR-0002](docs/adr/0002-session-identity.md) |
| 冪等性 | 同一 slug の tmux セッションがあれば**新規に立てず既存を返す**。**照合は cwd へ移す**（改名すると同じ作業ツリーに 2 本目が立つ。**I1 で実装済み**） | design.md §4.3、ADR-0002 |
| trust | ~~固定枠 `~/.cc-scratch/{1..8}`~~ → **一意なディレクトリへ**（ADR-0001）。自動承認は **ghq 配下**と**作業枠**（条件は「空」から「**ccs の印以外に何も無い**」へ。ADR-0002）。**`send-keys` で答える案は採らない**。**I2a → I2b → I2c で実装完了**（2026-08-31） | design.md §4.4、ADR-0001、ADR-0002 |
| 使い捨て枠の指定 | 正式な綴りは **`--tmp`**。予約語 `tmp` も残すが、ghq に同名リポジトリがあれば選ばずに止まる | design.md §4.3 |
| worktree | 置き場所は **`<repo>/.worktrees/<branch-slug>`**（`.git/info/exclude` で塞ぐ）。素性は **git から引く**（パス規約を同一性の根拠にしない）。`CCS_WORKTREE_ROOT` は**削除済み**。撤去は **`ccs gc` が 4 分類**で扱い、**`--force` / `-D` は使わない**（W3） | [ADR-0003](docs/adr/0003-worktree-under-repo.md) |
| v1 の範囲 | `new` / `ls` / `attach` / `kill` / `gc` まで。~~**汎用の `resume` と worktree は入れない**~~ → どちらも後から入れた（worktree は C1、`restore` は R1） | design.md §6、§9.6、§10 |
| hub | 常時 1 本。RC 名まで明示し、`bridgeSessionId` が付いて初めて「立った」。再起動は既定でまっさら | design.md §8 |
| 設定 | env > 設定ファイル > 既定値。**設定ファイルを source しない**。知らないキーは警告する | design.md §8.2 |
| 通知 | hub の異常は `hub.log`（JSONL）にだけ書く。**外部送信はしない** | design.md §8.1 |
| テスト | integration では **本物の `claude` を起動しない**（fake claude スタブを使う） | AGENTS.md |
| 言語 | POSIX sh。依存は `tmux` と `jq` のみ | design.md §6 |
| ccb との境界 | `ccs` は**タスクを知らない**。紐付けは `--label` の不透明な文字列で運ぶ。tmux を触るのは `ccs` だけで、ポーリングするのは `ccb` 側 | design.md §9 |
| `ls` の範囲 | **`ccs ls` は ccs 管轄（`cc/`）のみ。`--all` は作らない** ── **行が `kill` / `gc` の対象と一致していることが、いまの安全性の根拠そのもの**（`cc/` であることが「ccs が立てた」証拠。design.md §4.2・§10.1）。管轄外を混ぜると、人の判断も `ccs ls --json \| jq` 系も他人のセッションを巻き込む方向に倒れる。**俯瞰は別動詞**（A3） | 2026-08-30 の判断 |

## 着手可能

| # | 内容 | サイズ | 依存 |
| --- | --- | --- | --- |
| P0 | **使い捨て作業枠に `CLAUDE.md` を置き、「成果物は cwd に置く」と伝える**（[ADR-0004](docs/adr/0004-scratch-promotion.md) 決定 1）。**穴の本体はここ** ── ハーネスは一時ファイルを `/tmp` 配下の scratchpad に置くよう指示するので、**「一時的な作業」と言われたエージェントはそちらを選ぶ**。`~/.cc-scratch` は消えないのに、そこに立てたセッションの成果物が消える（[#63](https://github.com/ken-ty/ccs/issues/63) の実例）。**hub が既に同じことをしている**（`ccs hub up` が `CLAUDE.md` と権限設定を置く）ので、新しい仕組みは要らない。**昇格（P1b）より小さく、先に効く** | S | — |
| P1b | **昇格コマンドの実装**（[#63](https://github.com/ken-ty/ccs/issues/63)、[ADR-0004](docs/adr/0004-scratch-promotion.md)）。移す先は `~/ghq/local/<owner>/<name>`、`git init` まで `ccs` がやる、枠は空にして返す、セッションは `--resume` で立て直す（**cwd を跨げることは実測済み**）、**`gh repo create` はやらない**（打つコマンドを出すだけ）。**未決 1 が決まってから**（[#94](https://github.com/ken-ty/ccs/issues/94)） | M | #94、P0 |
| R3 | **再起動後の復帰が「戻しすぎる」件**（[#89](https://github.com/ken-ty/ccs/issues/89) の 2）。`ccs restore --yes` は「7 日以内に触った ccs 由来の会話」を全部立てるので、**再起動前に生きていた組だけ**が欲しいときは `--last` が要る。運用で使い分けるのか、既定を変えるのか、予告に併記するのかを決める。**人の判断待ち** | S | #89 |
| R4 | **畳んだ作業枠が 7 日間ずっと候補に残る件**（[#89](https://github.com/ken-ty/ccs/issues/89) の 3）。I2b で「枠が `gc` で消えても会話は戻せる」ようにした結果でもあるので**意図どおり**だが、使い捨ての枠が候補に居座るのが妥当かは別の話。**人の判断待ち** | S | #89 |
| H7 | **本物の claude で hub を 1 周する**の残り（docs/hub.md「まだ実測できていないこと」）。**アプリ側の見え方だけが残っている** — 名前が維持されるか / `offline` が積み上がらないか / `remoteControlAtStartup` との二重登録。launchd の経路は 2026-08-22 に実測済み。**`--resume` で RC が張り直されることは R1 の検証で確認した**（レジストリ上は同じ `bridgeSessionId` が戻る。アプリ側の見え方は未確認） | S | **人が手で回す**（本物の claude が要る。docs/hands-on.md をなぞる作業なので、ループの成果物にならない） |

## v2 の候補（着手しない。判断待ち）

v1 の範囲外と決めたもの。**拾う前に人の判断が要る。**

いまは空（V1 は R1 として実装した）。

## 完了ログ

| 日付 | 内容 |
| --- | --- |
| 2026-08-31 | P1a **作業枠の成果物の昇格を設計し、[ADR-0004](docs/adr/0004-scratch-promotion.md) に落とした**（[#63](https://github.com/ken-ty/ccs/issues/63)）。**コードは書いていない。****実測が 2 つ設計を決めた** ── (1) **`claude --resume <uuid>` は cwd を跨げる**（別ディレクトリへ中身を移して打っても会話がそのまま復元された）ので、昇格しても同じ会話を続けられる。セッションの張り替えは「立て直す」だけでよく、`restore_launch` と同じ手が使える。(2) **会話ログは古い場所に残る**（レジストリの cwd だけ新しくなる）ので、**`ccs restore` の索引が古い枠を指し続ける** ── ここを手当てしないと昇格は完成しない（未決。[#94](https://github.com/ken-ty/ccs/issues/94)）。**いちばん効く決定は「根治を先に置く」** ── 穴の本体は「エージェントが `/tmp` を選ぶこと」であって、成果物が枠に残らないことではない。**昇格は事後の救済で、次に同じことが起きるのを止められない**。枠に `CLAUDE.md` を置く（hub が既にやっている手）ほうが小さく、先に効く → **P0** として昇格より前に立てた。ほかに決めたこと: 移す先は `~/ghq/local/<owner>/<name>`（**既にその形で使われている**。実測: `local/ken-ty/goita-proto`）/ `git init` まで `ccs` がやる（`ghq list` は `.git` を見るので、リポジトリにならないと「local に昇格した」が意味を持たない）/ 枠は空にして返し symlink は残さない / **`gh repo create` はやらない**（外部への送信は人の承認が要る。打つコマンドを出すだけ）|
| 2026-08-31 | I3b **`ccs restore` の候補列挙を締めた**。2 つ直した。**(1) 作業枠は、ディレクトリが在るなら印まで見る** ── scratch root の直下にあるだけでは `ccs` が発行したものとは言えず、人が手で作ったディレクトリで会話していればそこも候補に出ていた。**消えている場合は素性を問わない**（印はディレクトリと一緒に消えるので、そこを疑うと `ccs gc` のあとに戻せなくなる ── I2b でわざわざ拾えるようにしたところ）。**(2) ghq 配下の「ccs 由来」の判定を厳しくした。** ここは**印ベースに置き換えられない** ── [ADR-0002](docs/adr/0002-session-identity.md) 決定 5 が「git の作業ツリーに印を置かない」と決めている（`.ccs.json` は `git status` に出て、コミットされ得て、clean 判定を壊す）。だから読めるのは**名前が付いていたことの痕跡**だけ。いままで「1 行目に `custom-title` を含むか」だけを見ていたので、**`ccs` 以外が `-n` を付けて起動したセッションも通っていた**。`customTitle` の値が **`ccs` がそのパスに対して計算する slug と一致するか**まで見るようにした ── slug は決定的に計算できるので、一致すればほぼ断定できる。外れるときは「候補に出さない」側に倒れる。**実機で誤検出が 1 件消えた** ── `bglab_web` の 1 行目は `bglab webサイト` で slug と不一致（＝ ccs 由来ではない）だったのに、以前は候補に並んでいた。**関数名は変えたが「印」とは呼ばない** ── これは痕跡であって印ではない、とコメントに書いた。テスト 4 件（integration）|
| 2026-08-31 | I3a **`ccs gc` の作業枠を素性で 4 つに分けた**（[ADR-0002](docs/adr/0002-session-identity.md) 決定 7）。**印だけ**（消す）/ **印と中身**（報告だけ。`issuedAt` と `issuedSlug` を添える）/ **稼働中**（触らない）/ **素性不明**（印が無い・壊れている・`workspaceId` が食い違う。報告だけ）。**本体は「空なら消す」をやめたこと** ── 以前は**人が手で作った空ディレクトリまで消していた**。`ccs` が発行したものではないので、そこに何が置かれるかを `ccs` は知らない。**印を失っても安全側に倒れる**（決定 7）── 消えたら「`ccs` のものではない」と見えるだけで、勝手に消されることも勝手に信頼されることも無い。**ADR 内の矛盾を 1 つ見つけた** ── 決定 7 は「印が無ければ触らない」、決定 8 は「レガシー枠は空なら `rmdir`」と言っている。**消すのは取り返しがつかないので決定 7 を採り**、[#91](https://github.com/ken-ty/ccs/issues/91) に上げた。結果として `~/.cc-scratch/{1..8}` は空になっても消されなくなる（報告はする）。既存テスト 2 件が旧セマンティクス（印の無いディレクトリを作って消えることを期待）を固定していたので、**ccs が実際に発行するもの＝印つき**に合わせた。テスト 6 件（integration）|
| 2026-08-31 | **`ccs restore` の生死判定を場所で行うようにした**（N1 の取りこぼし。実機で見つかった）。**症状**: 再起動後に hub へ「今までのセッションを立て直して」と頼むと、**身に覚えのないセッションがいくつも立つ**。調べたら、そのうち 3 本は**いま生きている worktree セッション**だった ── N1（#85）で slug の区切りを `@` から `--` に変えたので、`ccs` が組み立てる slug（`ccs--ls-board`）と、tmux 名・レジストリの `tmux` 欄（どちらも `cc/ccs@ls-board` のまま）が食い違う。**名前を起点にした照合が 2 つとも外れる**ので「居ない」と判断され、`--yes` を打てば**同じ会話に 2 本目の claude が立つ**ところだった。**I1 で他の経路を cwd に寄せたときの取りこぼし** ── restore の仕分けだけ `session_is_live`（名前起点）のままだった。`path_has_live_session`（cwd で照合。**管轄外の claude も数える**）を足して、名前のほうは「ペインは在るがレジストリに cwd が無い」ときの保険として残した。**名前の付け方を変えるたびに壊れる場所を1 つ減らした**のが本質。テスト 2 件（integration、両方向を stash で確認）。**この調査で分かった「ごみ」は 3 種類**で、他の 2 つはバグではない（[#89](https://github.com/ken-ty/ccs/issues/89) に整理） |
| 2026-08-31 | I2c **trust の自動承認を印ベースにした**（[ADR-0002](docs/adr/0002-session-identity.md) 決定 4）。これで **I2 系（a/b/c）が完結**し、ADR-0001・0002 の同一性の決定がひととおり実装に入った。条件は 3 つ ── 枠の直下 / **`ccs` の印がある** / その印以外に何も無い。**守っていたのは「空であること」ではない** ── ADR-0001 が守ろうとしたのは「`ccs` が作ったもので、まだ誰も何も置いていない」ことで、「空」はそれを外から確かめるための**代理指標**だった。印が入った以上、代理指標を直接の条件に置き換えられる。**判定は今までより厳しくなる** ── **scratch root の直下に人が手で作った空ディレクトリは、もう自動承認されない**（`ccs` が発行したものではないので、そこに何が置かれるかを `ccs` は知らない）。ADR がそう決めているとおり、安全側なので受け入れる。**レガシー枠（`{1..8}`）にも印は無い**ので承認されないが、実害はほぼ無い ── 一度でも使った枠は `~/.claude.json` に信頼済みとして残っており、`ensure_trust` はそもそもこの判定へ来ない。**新しい厳しさを捕まえる test は 1 件だけ**（旧コードで落ちるのはそれ） ── 残り 4 件は回帰ガードで、`dir_is_empty` が既に不正な印を弾いていたぶん両方向で通る。テスト 6 件（integration）|
| 2026-08-31 | I2b **使い捨て作業枠を一意なディレクトリで発行し、印を刻むようにした**（[ADR-0001](docs/adr/0001-scratch-workspace-identity.md) 決定 1、[ADR-0002](docs/adr/0002-session-identity.md) 決定 1・2、[#81](https://github.com/ken-ty/ccs/issues/81)）。`~/.cc-scratch/<workspaceId>` を発行し、**mkdir → 印 → trust → 起動**の順で通す（印には発行時の uuid を書くので、uuid はそこまでに決まっている必要がある）。**上限は残して意味を読み替えた** ── 「空き枠の本数」から「**同時に生きている本数**」へ、既定は 20。数えるのはレジストリを舐めて cwd が scratch root の直下にある生きた claude で、**管轄外のものも数える**（そこで動いている以上、資源は同じように食っている）。**発行を `resolve` から `ccs new` へ移した** ── `ccs resolve --tmp` に副作用があると、様子を見るたびにディレクトリが増える（worktree を遅らせたのと同じ理由）。**打ち方では判定しない** ── `--tmp` でも予約語の `tmp` でも同じ場所に落ちるので、解決した側が `CCS_RESOLVED_SCRATCH` を立てる（最初 `_want_scratch` で分岐して、`tmp` のときだけ**ディレクトリが作られずセッションが $HOME で立つ**バグを踏んだ）。**退行を 1 つ塞いだ** ── `restore` の候補列挙を枠の glob にすると、**`ccs gc` が消したあとの枠を拾えなくなる**（番号なら「消えていても番号は分かる」が、id は消えたら誰も知らない）。会話ログの置き場所は cwd から**前向きに組み立てられる**ので、そちらを起点にした。**テストの書き換えが本体の 3 倍あった** ── `tmp-1` を前提にした assertion が 88 箇所。id 非依存に寄せ、`_new_tmp` は発行順に slug を積んで `_ts <n>` で引く形にした（コマンド置換ごしなので変数では運べない）。テスト: 既存の大幅書き換え + 印の検証 4 件 |
| 2026-08-31 | N1 **worktree の slug から `@` を外した**（[#82](https://github.com/ken-ty/ccs/issues/82) で A を選択）。組み込みの `SendMessage` にとって `@` は **`name@team` のチーム区切り**なので、`ccs@ls-board` は「チーム `ls-board` の `ccs`」と解釈され宛先として弾かれていた ── **送る側からは見えているのに届かない**ので、worktree のセッションだけハブから指示を受け取れなかった。**着手前に実測した**（項目の指示）── `ListAgents` の 37 行のうち **ref を持たないのは `@` を含む 1 行だけ**で、空白・日本語・`#`・`/`・`[` `]` を含む名前はすべて通っていた。**効いているのは文字集合ではなく `@` そのもの**。区切りを **`--`** にし、**併せて `sanitize_slug` の許可集合からも `@` を外した** ── 区切りを変えるだけでは足りず、`foo@2` という名前のディレクトリを指されたら同じ壊れ方をする（実際、その挙動を固定していた unit test が落ちた）。**打ち方（`ccs new <repo>@<branch>`）は変えていない** ── あれは `SendMessage` を通らないので変える理由が無い。**波及は当初の想定より小さかった** ── 項目には「tmux 名と冪等性の照合に波及する」とあったが、I1（#72）以降その照合は cwd と uuid で行うので名前を変えても壊れない。残ったのは「名前は attach / kill の宛先である」の一点だけ。**既に立っているセッションの名前は変わらない**（ccs は改名しない）ので、`cc/ccs@ls-board` は立て直すまでハブから届かないまま。テスト 5 件（unit 1 + integration 3 + 既存の書き換え）|
| 2026-08-31 | I2a **印（`.ccs.json`）を読む層を入れ、「空であること」を印と両立させた**（[ADR-0002](docs/adr/0002-session-identity.md) 決定 2・4 の前半）。**入れたのは読むほうだけ** ── 書くほうは呼び出し元が無いと CLI から確かめられないので、**未検証のコードを置くより**最初の発行（I2b）と一緒に出すことにした。**`dir_is_empty` が正しい印を数えないようにしたのが本体** ── ADR-0001 が守ろうとしたのは「**知らないコードがそこに無い**」ことで、「空であること」はその代理指標だった。`ccs` が書いた印は知らないコードではないので、代理指標のほうを緩める。これをやらないと、**印を置いた瞬間に枠が「空でない」に化けて、自動承認も枠の再利用も止まる**（ADR-0002 の「印を置くと『空であること』と衝突する」）。**緩めるのは正しい印だけ** ── `kind` が `scratch`、`workspaceId` が**ディレクトリ名と一致**、`schema` が整数、の 3 つを見る。一致まで見るのは、**素性が「そこに書いてあること」ではなく「そこと結びついていること」**で決まるから（印ごとコピーされたディレクトリが元のものを名乗れてはいけない）。**波及を 1 つ踏んだ** ── `ccs gc` の空枠の掃除は `rmdir` なので、印が入ったディレクトリで失敗して「空だと言ったのに消せない」で止まる。**検証を通った印そのもの 1 つだけ**を先に `rm -f` してから `rmdir` に渡す形にした（`rmdir` は「そのどちらでもなかったときに止まる」門として残す。`rm -rf` にはしない）。テスト 8 件（integration）|
| 2026-08-31 | **I2 を S 3 つに割り、着手できない項目の判断を Issue に上げた**（コードの変更なし）。S の実装項目が尽きたので、ループが次に何を拾えるかを整えた。**I2 → I2a（印を読み書きする層）/ I2b（一意なディレクトリで発行）/ I2c（trust の自動承認を印ベースへ）**。順序に必然があるので依存に書いた ── **I2c を先に入れると、印を持たない新規枠が信頼されず起動時に必ず止まる**。I2a だけは他に影響しないので、書式の確定を先に済ませられる。**判断が要るものを 2 件 Issue へ** ── [#81](https://github.com/ken-ty/ccs/issues/81)（一意な枠にしたとき「増やしすぎの歯止め」を何にするか。`CCS_SCRATCH_SLOTS` と C5 の `scratch-full` が連動する）、[#82](https://github.com/ken-ty/ccs/issues/82)（worktree の slug の区切り。design.md §4.2 に触る）。**ルールを守れていなかったことの是正でもある** ── 「判断はチャットではなく Issue」と ROADMAP の冒頭に書いてあるのに、数サイクル続けてチャットで聞いていた。ループは非同期なので、チャットに書いた質問は誰にも届かない。冒頭の注意書きにその経緯を足した。**H7 は「人が手で回す」と依存欄に明示**（本物の claude でアプリ側の見え方を実測する作業なので、ループの成果物にならない）|
| 2026-08-31 | G1 **`ccs gc` が squash マージを「入っている」と見なせるようにした**。squash はコミットを作り直すので枝の先端が本体の祖先にならず、`git branch --merged` だけでは**squash 運用のリポジトリで消す候補がほぼ発生しない**（このリポジトリ自身がそれで詰まっていた）。**`git cherry` / patch-id は使わなかった** ── あれは**コミット単位**で突き合わせるので、**複数コミットを 1 つに潰した squash では当たらない**（実測: 2 コミットの squash で `+` が 2 本残る）。当たるのは元が 1 コミットのときだけで、いちばん拾いたい形が拾えない。代わりに**内容**を見る ── 「枝が触ったファイルのどれについても、本体と枝で中身が違わない」。**判定を緩める変更なので、着手前に 5 通りを実物の git で確かめた**（項目にそう書いてあった）: squash 済み＝拾う / squash 後に本体が別ファイルを触った＝拾う / 未 merge＝拾わない / **一部だけ cherry-pick＝拾わない** / squash 後に本体が同じファイルを触った＝拾わない。**外れるときは「消せるものを消さずに報告する」側にしか倒れない**。**危険側に倒れる経路を 1 つ潰した** ── ファイル名の一覧を `--` の後ろに並べると、空白を含む名前が分割されて**存在しないパス**になり、`diff --quiet` が「差が無い」＝ merge 済みと答える。2 つの名前一覧を**行として**突き合わせる形にして、名前の中身に触らないようにした（空白入りのファイル名でも拾わないことを実測）。報告には**どちらの根拠か**を出す（`merge 済み` / `内容は本体に入っている`）── 緩めた判定で消すので、なぜかが読めないと困る。テスト 5 件（integration）|
| 2026-08-31 | C4 **`ccs new --session-id <uuid>` と `--prompt-file <path>`**（design.md §9 の ccb 向けインターフェースの最後）。`--session-id` は**起動を待たずにハブ側でその会話の行を作れる**ようにするもの。8-4-4-4-12 の 16 進を `case` で見る（**検査 1 つのために `grep` を起こさない** ── `ccs new` はハブが繰り返し叩く入口なので、増やした分がそのまま毎回の費用になる）。大文字は弾かずに小文字へ寄せる。**既に使われているuuid では立てない** ── 同じ会話を 2 本の claude が握ることになる（`ccs adopt` の門と同じ理由。design.md §11.2）。`--prompt-file` は**呼ぶ側の argv に数 KB を載せないため**のもので、ccs が読んで `shq` で包む（内側の引用は ccs が持つ）。`-- <初期プロンプト>` とは併用しない── どちらが効いたのかを打った側が確かめられなくなる。失敗は C5 の枠組みに乗せて `session-id-in-use` / `prompt-file` を足した。**アンカーが 2 か所にあって一括適用が途中で止まった** ── `cmd_new` と `cmd_adopt` が同じ 3 行を持っている（C2 で `adopt` を書いたのは自分なので、以後 `cmd_new` を触るときは一意なアンカーを選ぶこと）。テスト 8 件（integration）|
| 2026-08-31 | C3 **`ls --json` に `startedAt` / `transcript` / `worktree` を足した**（`status` は元からあり、`pid` は `-l`、`labels` は C2）。**どこに足すかは「既定を軽く保つ」で決めた** ── 3 つとも**新しいプロセスを増やさない**ので既定の `--json` に入れた。`startedAt` はレジストリから、`transcript` は cwd と sessionId から組み立てるだけ、`worktree` は `path_is_linked_worktree` が**安い門番**（`.git` が**ファイル**か）を先に置いているので大多数の行が git を起こさずに落ちる。`worktree` は **git に訊く。パスの形からは判断しない**（ADR-0003 決定 3）ので `{"repo":…,"branch":…}` を返す。**`transcript` は止まっていても残す** ── 置き場所は cwd と sessionId から決まるので、戻る手掛かりを消さない（`startedAt` のほうは引けないので `null`）。行の欄が 5 → 6 に増えたので、`ls_board_rows` と `ls_as_table_long` の読み手も揃えた。既定のキー集合を守るテスト 2 件は更新。テスト 4 件（integration）|
| 2026-08-31 | C2 **`ccs new --label k=v`**（反復可）。ccb がタスクとセッションを紐付けるための**不透明な文字列**を運ぶだけで、**`ccs` は中身を解釈しない**（design.md §9。知らせると4 責務が 5 つ目に膨らむ）── 検査するのは「key があること」だけで、値には空白も `=` も日本語も入る。**1 つの tmux ユーザオプションに JSON で入れる** ── key ごとにオプションを分けると、読むときに**セッション 1 本につき 1 回 tmux を起こす**ことになる（`ls_rss_table` と同じ理由で避ける）。1 つにまとめれば `list-sessions -F` の 1 回で全部引けるので、`ls --json` に列を足しても「既定は軽い」の約束が破れない。組み立ては `jq`（必須の依存）に任せる ── 値に `"` が入るので自前の引用は必ず壊れる。**渡した key だけを上書きし、他は消さない**（「いま渡したものが全部」にすると、ラベルを知らない側が `ccs new` を打つたびに紐付けが消える）。置き場所が tmux なので**セッションを畳めば一緒に消える** ── ccs は状態を持たないまま。**罠を 2 つ踏んだ** ── (1) 改行の検査を `case $x in *"$(printf '\n')"*)` と書いたが、**コマンド置換が末尾の改行を落として空文字になり**、`*""*` が全部のラベルを弾いた（改行は変数で持つ）。(2) `_lbl=$(labels_table)` はtmux セッションが 0 本のとき `set -e` でスクリプトごと終了する（`|| true` が要る）。既定のキー集合を守るテスト 2 件は `labels` を足した形に更新。**一括取得は CI でだけ効かなかった** ── `#{@…}` の展開は tmux の版に依り、CI の 3.4 では `list-sessions -F` から空しか返らない（手元の 3.7b では引ける）。**片方に賭けず `or` にした** ── 一括で引けなければそのセッションだけ直接訊く。効かない版でも増えるのは**セッション 1 本につき tmux 1 回**で、`ccs ls` が既に払っている`pane_session_id_for` と同じ桁に収まる。**題材の非 ASCII でもう 1 度踏んだ** ── ラベルの値に日本語を使ったテストが CI でだけ落ちた（I4 の改名先とまったく同じ環境要因。`LC_ALL=C` の Linux で往復しない）。**同じ罠を 1 セッションで 2 度踏んだ**ので、題材の非 ASCII は「見たいものがそれ自体でない限り使わない」と考えたほうがよい。代わりに `#{}$(x)`y`` を運ばせて、**tmux の format も shell の展開も起きない**ことを見るようにした。テスト 8 件（integration）|
| 2026-08-31 | I4 **hub の生死判定も名前ではなく cwd と uuid で行うようにした**（I1 系の最後の 1 つ）。`hub_status` は `tmux_session_exists "cc/<hub-slug>"` で見ていたので、**hub を改名すると `absent` になり、`ccs hub up` が 2 本目を立てた** ── hub は 1 本であることが前提の仕組みなので、2 本立つとどちらに指示が届くかが運任せになる。**途中で 1 つ学び直した** ── 最初は cwd から引いたレジストリの `tmux` 欄を宛先に使ったが、**あの欄は起動時のスナップショットで改名に追随しない**（この調査の最初に実測していたのに、実装で踏んだ）。素性を引くのには使えるが、いま届く宛先にはならない。宛先はペインの起動コマンドに残る **uuid** から引く（`tmux_session_for_uuid`、`--resume` で立て直したペインでも同じ形で残る）。**同じ穴が I1 の `ccs new` にもあった** ── 改名された相手を見つけたときに起動時の slug を返していたので、`ccs attach` できない slug を返していた。こちらも uuid 経由に直した（test の期待値も更新）。置き換えで `live_bridge_for` が未使用になったので削除。**CI でだけ落ちた** ── cwd の照合だけにしたら ubuntu で hub が「居ない」ことになった（手元の macOS では、サンドボックスを `/tmp` に揃えても再現せず。原因は tmux か OS の差まで絞れたが**特定していない**）。**片方に賭けず `or` にした** ── cwd の照合は改名を拾えるぶんの上積みで、効かない環境でも元の名前引きがそのまま残る（I1 の占有判定と同じ原則で、今までより緩くならない）。`tmux_session_for_uuid` も `list-panes -a` の 2 欄を自前で割るのをやめ、既に CI で通っている `pane_session_id_for` に寄せた。**CI だけで落ちた本当の原因はテストの題材だった** ── 改名先に `cc/朝会夕会` を使っていたが、`make check` が強制する `LC_ALL=C` の下では**非 ASCII の tmux セッション名が Linux で往復しない**（名前を `list-sessions` で読み、`has-session -t` へ渡し直す経路がここにある）。**CI に診断値を出させて確定した** ── ASCII 名で同じ手順を踏ませたら `state healthy` / `tmux cc/renamed` と正しく動いていた。見たいのは「改名されたこと」であって「日本語であること」ではないので、題材を ASCII に寄せた。**手元では通り CI でだけ落ちる**種類なので、`identity.bats` の日本語の改名（名前を読み返さない経路なので通っている）と紛らわしい。テスト 4 件（integration）|
| 2026-08-30 | C5 **`ccs new` の失敗も機械可読にした**。stdout は元から機械のためのもの（成功すると JSON が出る）なのに、**失敗したときだけ何も出ず**、読む側には終了コードしか手掛かりが無かった ── 「曖昧で選べない」のか「trust で固まった」のかが区別できない。**出口を 1 つにする** ── `ccs new` が失敗する場所は target の解決から登録待ちまで散っていて、1 か所ずつ JSON を足すと**必ず取りこぼす**（足したあとに増えた出口が漏れ、漏れたことに気づく手立ても無い）。代わりに **EXIT の trap** で、終了コードが 0 でないときだけ 1 行出す。`code` は `usage` / `scratch-full` / `stale-pane` / `not-registered` / `fail` の 5 つで、**ハブが「打ち直しで直るか」を判断できる**粒度にした。**文言は人間向けのものから作る**（2 つ書くと必ずずれる）。`die` が素性を覚え、具体的な場所は `error_is` で先に立てておけば`die` は上書きしない。**成功したときの形は変えていない**（`error` を足さない。回帰ガード付き）。テスト 7 件（integration、うち 6 件が旧コードで落ちることを stash で確認）。**ついでに I1 系の棚卸しを 1 度やり切った** ── 残っていたのは hub の生死判定だけで、I4 として積んだ |
| 2026-08-30 | R2 **`ccs restore --json`**。ハブのエージェントが結果を読めなかった。**1 つのオブジェクトで返す** ── `applied` で分岐すれば足り、人間向けの 3 つの見出し（立て直せる / 生きているので触りません / 戻せません）を読み分けさせない。**空のときだけ別の形にしない**（読む側が 2 通りの分岐を持つことになる）、**`--yes` を付けたら必ず 1 つ返す**（黙って終わると「失敗したのか対象が無かったのか」を区別できない）、**stdout は JSON だけ**（案内は stderr。混ぜるとパースできない）。`--list --json` は会話ログを `conversations` に並べる。**JSON は既定の出力と同じ材料から作る** ── 別経路で組み立てると人が見る表と機械が読む JSON がずれていく。**ついでに I1 の穴を 1 つ塞いだ** ── `restore` の生死判定も名前引きのままで、改名された稼働中のセッションが候補に出ていた。`--yes` を打つと**生きているペインを畳んで立て直す**（会話は残るが走っていた作業は巻き添え）。`session_is_live` に寄せて test を足した。テスト 8 件（integration、全件が旧コードで落ちることを stash で確認）|
| 2026-08-30 | N2 **`ccs ls` / `ccs agents` の表示も名前に依存させないようにした**（I1 の残り）。**破壊はしないが、いちばん目に付く症状はここだった** ── 改名すると `busy` で動いているセッションが `stopped` と出る。見た人は復帰の手を打とうとするので、**`ccs restore` で生きている会話をもう 1 本立てる**ところまで行きかねない。I1 で作った uuid 照合を `registry_file_for_session` として切り出し、`collect_session_row` と `session_is_live` の両方がそれを通るようにした（名前引きの重複ループも消えた）。**改名の救済が死んだセッションまで生かして見せない**ことは別の test で見張っている。`ccs agents` は同じ関数を通るので一緒に直り、レジストリの `tmux` 欄が `cc/` のままなので**管轄外の行に二重で落ちることもない**（回帰ガードとして test 化）。テスト 4 件（integration、うち 2 件が旧コードで落ちることを stash で確認） |
| 2026-08-30 | I1 **占有と冪等性を cwd で照合するようにした**（[ADR-0002](docs/adr/0002-session-identity.md) 決定 3）。tmux 名も claude の `name` も表示用のラベルなので、同一性の根拠にしない。名前で当てていると**改名されたセッションが「居ない」ことになり**、4 経路が同時に壊れていた ── `resolve_as_scratch` は**稼働中の枠を横取り**し、`cmd_new` は**同じ作業ツリーに 2 本目の claude** を立て、`orphan_slots` は**使われている枠を「空き」として消し**、そして `ccs gc` は**生きているセッションを畳んだ**。**4 つ目は ROADMAP の記述に無かったが、いちばん破壊的なので範囲に入れた** ── 実測で、改名した稼働中のセッションが `gc --yes` で消え、tmux サーバごと空になった。占有は「tmux セッションがある **or** その cwd に生きている ccs のセッションがある」の or で見る（**今までより厳しい側にしか倒れない**）。生死は**ペインの起動コマンドに残る uuid** をレジストリと突き合わせて決める（改名しても uuid は変わらない）。**数えるのは管轄下だけ** ── アプリや VS Code でその場所を開いているだけで `ccs new` が断られると使えない道具になる（引き取るなら `ccs adopt`）。比較の前に `abs_dir` を通す（`/tmp` と `/private/tmp` で2 度踏んでいる）。**テストが空振りしていないことを確かめた** ── 実装を stash して 6 件中 3 件が落ちること、戻すと通ることを両方向で確認（1 度目に書いた gc の assertion はサンドボックスに存在しない文字列を見ていて**旧コードでも通った**。実パスで見るよう直した）。**表示側（`ccs ls` が改名を `stopped` と出す）は破壊的でないので N2 として残した。** テスト 6 件（integration） |
| 2026-08-30 | A3 **`ccs agents [-l] [--json]` — このマシンの claude を俯瞰できるようにした。** **`ccs ls` には混ぜない**（設計方針の「`ls` の範囲」）── あちらの行が kill / gc の対象と一致していること自体が安全性の根拠なので、別の動詞に分けた。`claude agents` と同じ綴りにしたのは、「ccs の管轄の一覧」ではなく**「claude の母集団を ccs の目で見た表」**だと名前で分かるようにするため。**区別は SLUG の `-` だけ** ── 専用の列を足さず、`-` が「attach も kill も宛先が無い」を表す。管轄外の `name` を SLUG 欄に置かないのは、**打てない文字列が打てる位置に座る**ため（実測で空白・日本語・`/` を含む名前が付く）。並びは管轄ぶんが先で、**上半分が `ccs ls` と行単位で一致する**。JSON では管轄外の `slug` と `tmux` を `null` にする ── `-`（値が取れなかった）と `null`（そもそも無い）を分ける。`ccs ls --json` の出力は変わらない（あちらの slug が `-` になることは無い）。`status` は**無いことがある**ので`-` を置く（実測: アプリ由来のセッションに status キーそのものが無い。スタブに `FAKE_CLAUDE_NO_STATUS` を足して再現）。`ls_board_rows()` は slug を見ないので **`-l` は流用でそのまま通る**。**ヘルプに母集団を書く** ── 見えるのはこのマシンの claude だけで、`ListAgents`（39）とレジストリ（20）は数が違う。テスト 9 件（integration）、外部セッション役のヘルパは `test_helper.bash` へ寄せて adopt と共有。設計は design.md §12 |
| 2026-08-30 | A1 **`ccs adopt <target>` — ccs 管轄外のセッションを引き取れるようにした。** **プロセスは移せない**（実測: tmux セッションを rename してもレジストリの `tmux` 欄は追随せず、`busy` のセッションが `ccs ls` に `stopped` と出た。あの欄は起動時に 1 度書かれるだけ）ので、引き取りは**会話の引き取り**になる ── 元を閉じ、同じ `sessionId` を `--resume` で `cc/<slug>` のペインに開き直す。実行部は `restore_launch()` の流用。**門の本体は「元が生きている間は絶対に立てない」** ── 実測で `claude --resume` は**別プロセスが握っていても拒まず**、同じ sessionId のレジストリが 2 件並び、しかも**引き取った側は `bridgeSessionId` が `null`** になった（先に居るほうが RC を譲らない）。RC が無い ＝ アプリからもハブからも見えないので、**成功したように見えて見えないセッションができる**。閉じ方は **SIGTERM → レジストリファイルの消滅待ち**で、**SIGKILL に昇格しない**（書き切られていない会話を resume することになる）。待ちきれなければ**何もせず終わる**。**cwd の扱いで 1 つ踏んだ** ── 照合は正規化して緩く当てるが、会話ログの置き場所は cwd の**文字列**から決まるので、そこから先は**相手が名乗る綴りをそのまま使う**。正規化した綴りで立てると同じ会話が 2 か所に割れる（macOS の `/var` と `/private/var` で実際に踏んだ）。**テストの落とし穴も 1 つ** ── 外部セッション役の pid を変数に溜めていたが、コマンド置換ごしに呼ぶので代入がサブシェルに閉じ、teardown から見えずにプロセスが残り、**全 8 件が ok になったあと bats だけが返ってこない**形で現れた（子プロセスを待つため）。pid はファイルに書く。テスト 8 件（integration）、`ccs kill --self`（A2）が頼まれた側の受け皿。設計は design.md §11 |
| 2026-08-30 | A2 **`ccs kill --self` を tmux の外でも成立させた。** A1（`ccs adopt`）が「引き取るので閉じて」と頼んだときの受け皿 ── いままでは `kill_self_slug()` が `cc/` の中でしか成立せず、**管轄外のセッションは自分を閉じられなかった**。`--self` が言っているのは「自分が終わる」であって「ccs が立てたものを畳む」ではないので、**自分で名乗り出ている以上、管轄外に手を出すことにはならない**。自分の特定は**祖先の pid でレジストリを引く**（ファイル名が pid そのもの）。**`CLAUDE_PID` は使わない** ── claude は自分の pid を子へ渡すが（実測 2026-08-30）、env は継承されるので**入れ子や古い値で他人を指しうる**。ここは kill の宛先なので、取り違えると畳まれるのが他人のセッションになる。祖先を辿れば見つかったものは定義上その親なので、取り違えが原理的に起きない（テストで「祖先でないレジストリは掴まない」を見張っている）。畳み方は **claude の pid に SIGTERM**。**SIGKILL に昇格しない** ── SIGTERM なら会話ログを書き切るが（design.md §10 の実測）、KILL だと書き切られていない会話が残り `--resume` で戻す先が壊れる。**畳むことより戻せることのほうが大事**。未コミットの門も報告を先に出し切る順序も tmux の中と同じ。**`set -e` の罠を1 つ踏んだ** ── `kill_self_external` が 1 を返した時点でスクリプトごと落ち、`die` まで届かず終了コードが 2 ではなく 1 になった。`||` で受けて直した（テストが捕まえた）。テスト 5 件（integration） |
| 2026-08-29 | D2 **ccs を ghq のチェックアウトの外へ配布する**（版の論点 (b)。D1 の続き）。PATH の `ccs` は `~/ghq/…/ccs/bin/ccs` を指す symlink だったので、**走るコードは main checkout の HEAD がその瞬間に何であるかそのもの**だった。**「古い」より「可変」なのが本体** ── main で branch を切っただけでインストール操作なしに別物になり、`git pull` は実体をその場で書き換えるので原子性も巻き戻しも無い。形は同じマシンの `claude` / `cursor-agent` に倣った（`~/.local/share/ccs/versions/<版>` + symlink を **rename で差し替え**）。`ccs` 本体は自分を更新しない ── インストーラは `make install` / `scripts/install.sh` で、**自分の複製を `~/.local/share/ccs/bin/` に置く**（ghq のチェックアウトが壊れていても・別ブランチでも巻き戻せる。巻き戻しが checkout に依存したら「別 path で管理する」が徹底できない）。**核心は `git show origin/main:bin/ccs`** ── オブジェクトから直接読むので `pull` も branch 切り替えも要らず、**他のセッションが使っている作業ツリーに一切触らない**（`fetch` は ref しか更新しない）。手で `make install` したときだけ作業ツリーの `bin/ccs` を入れる（手元のビルドを実機で試す入口。dirty なら版に印が付き、並べて置かれるので 1 手で戻せる）。検知は **launchd から 5 分ごとに走っている `ccs hub up` に相乗り**する ── **新しいデーモンは作らない**（design.md §2.1）。ネットワークは `CCS_UPDATE_INTERVAL`（既定 1 時間）に 1 回。**検知したら自動で切り替える**（人の決定。M2「用意まで自動」を推したが M3 が選ばれた）。無人で入れ替わるので 5 つを満たす: 原子的な差し替え／**切り替える前にその版が `version` を答えるか試す**（人が見ていないので、起動もできないものを掴ませると hub ごと止まる）／`install.log` に「いつ・どの版から どの版へ・きっかけ」と `hub.log` に `updated`／前の版を残して 1 手で巻き戻せる／`CCS_AUTO_UPDATE=off` で止められる。**切り替えを実行しているのは切り替えられる当人（launchd が起動した古い版）**なので、記録に自分の版を書くと「何から何へ」を取り違える ── 記録はインストーラが切り替え後の版で書く。根拠は **`origin/main` の HEAD**（tag は節目にしか打たないので遅れる）。**CI の緑は条件にしない** ── launchd の経路から `gh` の認証を要求することになるのが 1 つ、`main` は ruleset で required check 付きなので**赤いものはそもそもマージできない**（origin/main は構造上すでに緑）。**確認できないときは「最新」と言わない** ── オフラインで ref を読めても今のリモートと同じとは限らず、そこで最新だと答えると「見ていないのに大丈夫だと言う」ことになる。ただし `hub up` の側では騒がない（5 分ごとに「分からない」と言われても人にできることは無く、肝心の変化が埋もれる）。読むだけの `ccs doctor` を足した ── 事故のとき、PATH の ccs が何を指しているかは `ls -l` を打つしかなかった。**テストで 2 度、空振りに気づいて書き直した**: 「起動できない版に切り替えない」は `CCS_BUILD` 行ごと壊した版で試していたため smoke test ではなく**焼き込み検査**のほうで弾かれており、「焼き込めない版を入れない」は逆に smoke test のほうで弾かれていた ── どちらも**狙ったガードを殺しても緑のまま**だった。`--auto` が作業ツリーに触らないこと・起動できない版に切り替えないこと・`CCS_AUTO_UPDATE=off` が効くことの 3 つは、**わざとコードを壊して落ちることを確かめてある**。設定キーを 5 つ追加（`CCS_INSTALL_ROOT` / `CCS_BIN_DIR` / `CCS_AUTO_UPDATE` / `CCS_UPDATE_INTERVAL` / `CCS_KEEP_VERSIONS`）。`make lint` は `scripts/install.sh` も見る（**入れる前に走る**ので、壊れていても ccs 本体より先には気づけない。壊れたインストーラは「入らない」ではなく「変なものを入れる」側に倒れうる）。**install 元のチェックアウトが消えた場合を詰めていて、実装のバグを 1 つ実測で見つけた**: 控えが辿れないときに「インストーラ自身が居るリポジトリ」へ落ちる作りにしていたので、install 元を消したあと ccs の作業ツリーから `--check` を打つと、**そちらの `origin/main` を「新しい版」として報告した** ── **検知の根拠が黙って入れ替わるのは、古さを見逃すより悪い**。控えてある install 元だけを見て、消えていれば場所を名指しして止まる形に直した（fallback は初回 install 専用に残す）。**巻き戻しはチェックアウトが無くてもできる** ── 複製と過去の版は `$CCS_INSTALL_ROOT` に残るので、チェックアウトが要るのは入れ直すほうだけ（複製を置いている狙いがこれ）。**手元では緑で CI でだけ 12 件落ちた** ── `git init --bare` が作る HEAD は `init.defaultBranch` 次第で、macOS の Apple Git は `main`、**ubuntu の既定は `master`**。テストが bare を作って `main` を push すると bare の HEAD は `master` を指したままになり、そこから clone しても**作業ツリーが空**になる。推測で直さず、`GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=init.defaultBranch GIT_CONFIG_VALUE_0=master` で**手元に 12 件の失敗を再現してから**、bare 作成直後に `symbolic-ref HEAD refs/heads/main` を打つ形に直した。**Makefile で既定を固定する案は採らない** ── 固定すると `make check` が runner の条件を二度と再現できなくなる。AGENTS.md の「手元の環境に依存させない」に 3 つ目として追記（既存はロケールと tmux）。テスト 39 件（unit 28 + integration 11）。合計 504 件（unit 196 + integration 308）。ドキュメントは [docs/versioning.md](docs/versioning.md) |
| 2026-08-29 | D1 **版をコミットから導出する**（版の論点 (a)）。発端は事故 ── PATH の `ccs` が 5 コミット古く `ccs restore --last` が「知らないオプション」で落ちた。**そのとき `ccs version` は診断に使えなかった**: 手書きの `CCS_VERSION` は `0.1.0` → `0.2.0` → **`0.0.1`**（docs だけの PR [#44](https://github.com/ken-ty/ccs/pull/44) で**後退している**）→ `0.0.3` と動いたあと #49 / #61 / #62 をまたいで `0.0.3` のまま止まっており、**`--last` を持つ版と持たない版に同じ答えを返していた** ── 「前」と「後」を区別できない番号は版として機能しない。解決は 3 段（焼き込み `CCS_BUILD` → 自分の在処の git から導出 → `unknown`）。**焼き込む側（install）はまだ無い**ので、ここで用意したのは置き場所と、埋まっていないときに git から導出する経路（→ D2）。**種別を綴りから当てない** ── 短い sha は数字で始まることがある（実測 `3f5673a`）ので「数字で始まれば tag 由来」は誤判定する。`--always` を付けずに `git describe` させ、**成否そのもの**を種別にした（tag が 1 つも無ければ describe は失敗する）。以前の `X.Y.Z` の契約は `--short` へ移し（機械可読の入口を消さない）、既定の出力は `0.0.3+3f5673a` の形に。焼き込みが無ければ `(dev)` を付ける ── 手元の worktree を叩いているのか入っているものを叩いているのかは、食い違っているときに真っ先に知りたい。`unknown` は「分からない」と言わせる（黙って錨の番号だけ返すと、また「前と後が同じ」に戻る）。**自己レビューで挙げた指摘のほうが誤りだった 1 件**: 「`$0` が裸の名前なら cwd の版を答えてしまう」と直したが、**テストが修正前でも通った**ので調べ直したところ、shebang のスクリプトは**カーネルがインタプリタにパスを渡して**起動するので `$0` は必ずパスになり（`exec -a` で argv[0] を偽装しても変わらないことを実測）、その経路は存在しなかった。ガードを外し、テストは実際に検証している内容（PATH 経由・別のリポジトリの中から打つ）に書き直した ── **空振りするテストを残さない**。`make lint` が 2 つ見張る: **コミットされた `bin/ccs` の `CCS_BUILD` は必ず空**（焼き込み済みをコミットすると、どのコミットに置かれても同じ版を名乗り続ける ── 手書き番号が腐ったのと**同じ壊れ方を自動化した経路で再現する**）／`CCS_VERSION` は `X.Y.Z` の形。tag・dirty・symlink 経由・git の外といった状態は、**使い捨ての git リポジトリを作って**試す（本物に tag を打つのは副作用なので、テストからは触らない）。テスト 14 件（unit）。合計 465 件（unit 168 + integration 297）。ドキュメントは [docs/versioning.md](docs/versioning.md) |
| 2026-08-28 | K1 `ccs kill --self` ── **セッションが自分で終わる口**。出発点は「アプリでアーカイブしても `ccs ls` に idle で残り、落ちない」という報告だが、**前提のほうが誤りだった** ── 実測（2026-08-28、動いている 16 本）でレジストリのキーを全部並べても `archived` に当たるものは無く、`status` は `idle` 8 / `waiting` 6 / `busy` 1 / `null` 1 の 4 値だけ。アーカイブは会話一覧の操作で **CLI のプロセスは生き続ける**ので、`ccs` は誤読しておらず idle は事実。検知する側から直すと `ccs` が claude の内部状態を推測することになり、「一覧と状態は claude に委譲する」（design.md §2.1）を裏返す ── だから**終わる意思のあるセッションが自分で終わる**形にした。**半分は既にあった**（`ccs kill` には自セッションを指したときの `--force` 必須ガードがある）が、足りないものが 3 つ: 自分を指すのに slug を打つ / **後始末が届かない**（`tmux kill-session` より後ろに置いた「同じ会話に戻るには」と worktree の案内は、自分を畳むと動かない）/ 未コミットの門が無い。**「殺してから片付ける」を作らない**のが本体で、集める → 断る → 報告を出し切る → **最後の 1 手として `kill-session`** の順に固定した。4 を `exec` にしてあるのは、**後ろに何かを足せなくするため**。working の門は `--self` には掛けない（この `ccs` を動かしているのが自分なので、自分の status は必ず作業中になり、掛けると常に断られる）。代わりに未コミットの門を置いた ── `kill` はファイルを消さないので「失われる」ことへの門ではなく、**終わると言っている本人に「本当に終わったのか」を訊く**門。hub は `--force` でも拒否。worktree は残す（`ccs kill` と同じ方針）。**後始末は claude 自身がやる**ことを本物で実測した: 1 セッションにつき `~/.claude/sessions/<pid>.json` / `<pid>.<hash>.key` / `/tmp/cc-socks/<pid>.sock` の 3 つができ、`tmux kill-session` の SIGHUP で **3 つとも消える**（sock が先、json と key は数秒遅れ ── 即座ではない）。`ccs` が消して回る必要は無く、先回りすると生きているプロセスの足元を抜く。自分で終わるところまでも本物で 1 周させた（`ccs new <target> -- "ccs kill --self を実行して"`）── `~/.claude/sessions` は 34 → 36 → **34**、`/tmp/cc-socks` は 17 → 18 → **17** に戻り、tmux セッションも残らなかった。**打った本人は報告を読めない**（ペインごと消えるので、出力を受け取るプロセスも一緒に死ぬ。実測で会話ログの末尾はツールの結果が書かれないまま終わっていた）── 読めるのは断られたときだけ、と割り切って docs に書いた。integration では「中から呼んだ」ことを `$TMUX` / `$TMUX_PANE` の env で作る（本当にペインの中から実行するとテストのプロセスが結果を見られない）。**「自分を殺したプロセスが最後まで走れない」ところだけは自動では再現できない**ので、そこは docs/hands-on.md §9.1 の手動確認が受け持つ。テスト 12 件（unit 3 + integration 9）。合計 451 件（unit 154 + integration 297） |
| 2026-08-28 | R3 `ccs restore --last` ── **前回の停止まで生きていた組だけ**を、引数の手打ち無しで戻せるようにした（`--since <期間>` も同時に）。実機の出発点は 2026-08-27 の再起動で、`ccs restore` の候補 19 本のうち戻したいのは 13 本、残り 6 本は数日前からの残骸だった ── **hub が目視で slug を 13 個並べて手打ちしていた。** **判定の根拠は「会話ログの mtime は最後に活動した時刻ではなく、最後に生きていた時刻」であること**（実測 2026-08-27）: `last reboot` の shutdown time は 20:50、`kern.boottime` は 23:22:44、20:50:08〜20:50:53 に 13 本の mtime が並び、**そのうち 2 本の最後の実内容は前日 8/26**（末尾は timestamp を持たないメタ行 `last-prompt` / `bridge-session`）。丸 1 日アイドルの会話が停止時刻の mtime を持つ＝OS のシャットダウンが SIGTERM を送り claude が書き切っている。**だから手で畳んだものだけが塊から外れる** ── 「手動 kill が混ざるのでは」という懸念はここで塞がる。単なる時刻クラスタリングではない。**スナップショットは持たない**（design.md §2.1 に反するのが第一だが、そもそも上のとおり mtime が既にスナップショット。仮に持っても書くタイミングが詰む ── 定期はデーモンが要り、shutdown フックは電源断・パニック・SIGKILL で走らない＝**走らない場面がまさに一番欲しい場面**）。塊の切り出しは、閾値の根拠を自前で作らずに**起動時刻を外の基準点として使う**: `mtime < T_boot` に絞る → 最大値 `T_last`（実質、停止の瞬間）→ `[T_last - CCS_RESTORE_LAST_WINDOW, T_boot)`。窓の既定は 5 分（実測のばらけ 45 秒に対する余裕）。`--last` は列挙の古さの線（`CCS_RESTORE_MAX_AGE`）を内部で外す ── マシンが 7 日以上落ちていたら塊ごと落とされるため。電源断・強制再起動では一斉書き込みが起きず `--last` が「単に直近に触ったもの」に劣化するので、逃げ道に `--since <数><s\|m\|h\|d>` を置いた（絶対時刻は受けない ── epoch へ直す綴りが BSD の `date -j -f` と GNU の `date -d` で割れ、**どちらの環境でも試していない分岐**が必ず残る）。既定は dry-run のままで、dry-run の案内は**絞り込みごと繰り返せる形**で出す（`ccs restore --last --yes`）── ここで素の `--yes` を案内すると、絞ったつもりの人が残骸まで立ち上げる。`hub up` の案内も `--last` を先に出す。実装で 2 つ踏んだ: `sysctl -n kern.boottime` を `.*sec *= *\(...\)` で拾うと **`.*` が貪欲で `usec` に当たり**、起動時刻ではなくマイクロ秒（903594）が返って候補が全滅する。`--since ''` は「値が空」と「`--since` 無し」が区別できず、**絞ったつもりが全候補**に化ける（`_has_since` で分けた）。テストの差し替え点に `CCS_RESTORE_BOOT_EPOCH` を足した（再起動を模せないと `--last` は検証できない）。テスト 17 件（unit 8 + integration 9）。合計 436 件（unit 151 + integration 285）|
| 2026-08-27 | B1 `ccs ls -l`（`--long`）で**盤面**を出せるようにした ── 直近の依頼 / RSS / 最終更新。**ハブが「答える前に盤面を取り直す」ための入口**で、それまでハブ側の暫定スクリプト（`~/.cc-hub/bin/board`）が `ccs ls --json` の上に手続きを積んでいたのを ccs へ寄せた。**既定の `ccs ls` は列も `--json` のキーも増やしていない** ── 会話ログを舐める分だけ確実に遅くなるので、「立っているものを知りたいだけ」の用途に払わせない（回帰テストで固定）。**空欄を出さないことと速さの両立がこの項目の本体。** 暫定スクリプトは末尾 300KB 固定で読んでいたため、ツール結果が続く会話では user 行に届かず 13 本中 4 本が空欄だった。**末尾 256KB → 4MB → 全部**と広げる形にして空欄を消した（実測: 立っていた 15 本で、末尾から最後の依頼までの距離は 9 本が 100KB 以内、最大 1.6MB）。**`grep` の速さを ugrep で測って 30 倍外した** ── 手元の zsh では `grep` が ugrep の別名になっていて 50MB が 0.019 秒で終わるので、当初は「全部 grep するほうが tail より速い」と結論していた。`ccs` が起動するのは `/usr/bin/grep` で、同じ 50MB が 0.58 秒かかる。**`grep -F` を 2 パターン並べるより ERE 1 本のほうが速い**（0.58 秒 → 0.30 秒）。ほかに、行ごとに `sed` / `date` / `awk` / `ps` を起こさない形へ寄せた（会話ログのパスは awk 1 回、`ps` は全 pid をまとめて 1 回、「いま」は 1 回）。人が打っていない定型文（`[Request interrupted by user]`・`<command-name>…`・`Caveat:`・スキルの注入・継続プロンプト）は候補から外し、1 つ前へ遡る。切るのは**文字数ではなく桁数**（全角は 2 桁。文字数で切ると日本語が横 140 桁になり 1 画面に収まらない）。**タブ区切りの行に空欄を作らない**という穴を 1 つ踏んだ ── IFS にタブを入れて `read` すると連続したタブが 1 つの区切りに潰れる（POSIX: タブは IFS の空白文字）ので、stopped なセッションの pid を空で運ぶと後ろの列が全部ずれて JSON が壊れた。無い値は `-` で運ぶ。**モデルは呼ばない**（テキスト処理だけ）。実測: 15 本（5 回）で `ccs ls` 0.37〜0.43 秒 / `ccs ls -l` 0.90〜0.96 秒。**負荷で振れる**（15 本の Claude が動いている時間帯には 1.2〜1.4 秒まで伸びた）。**残る 0.38 秒は既定の `ccs ls` が元から持っているコスト**（レジストリを slug ごとに舐める。ここは触っていない）。差し替え点に `CCS_PS_BIN` を追加。テスト 18 件、合計 419 件（unit 143 + integration 276）。ドキュメントは [docs/board.md](docs/board.md) |
| 2026-08-27 | **テストが後始末しきれず、ゴミを共有の場所に積み上げていた**のを構造ごと直した（[#50](https://github.com/ken-ty/ccs/issues/50) / [#51](https://github.com/ken-ty/ccs/issues/51) / [#52](https://github.com/ken-ty/ccs/issues/52) / [#53](https://github.com/ken-ty/ccs/issues/53)）。発端は **hub が見つけた亡霊** ── `test/integration/hub.bats` が立てた tmux サーバが、`cc/hub` を抱えたまま**丸 1 日以上生きていた**。tmux サーバは端末から切り離された常駐プロセスなので bats を殺しても端末を閉じても死なず、専用ソケットなので **`ccs ls` にも `ccs gc` にも出てこない** ── 誰も気づけない。調べると `/tmp/tmux-501/` に**ソケットが 3,950 個**溜まっていた（1 日弱ぶん）。**teardown の書き忘れではない** ── 8 ファイル全部に書かれている。実測した bats の挙動: 正常終了・SIGTERM・SIGHUP では teardown が走るが、**SIGINT（Ctrl-C）では走らず、SIGKILL では走れない**（`make check` は 6 分かかるので Ctrl-C の機会は十分ある）。4 段で塞いだ。**(1) ソケットの置き場所を tmux に決めさせない** ── `-L <名前>`（共有の `/tmp/tmux-<uid>/`）をやめて `-S <サンドボックス>/tmux.sock` にした。**テストが作るもののうち、そこだけがサンドボックスの外に出ていた**のが 3,950 個の正体で（`kill-server` はソケットファイルを消さない。tmux 3.7b で実測）、中に入れれば `ccs_teardown_sandbox` の `rm -rf` が持っていく ── **後始末に特別扱いが要らなくなる**のが要点。パス長は macOS の `$TMPDIR`（48 バイト）込みで実測 75 バイト、Unix ドメインソケットの上限 104 に収まる。**(2) 見張りを外のプロセスに置く** ── 持ち主（テストのプロセス）が消えたら畳む。プロセスの生死しか見ないので teardown が呼ばれるかと無関係に効く。落とし穴が 3 つあり、どれも外すと黙って効かなくなる: `trap '' INT`（**Ctrl-C はプロセスグループ全体に届く**ので巻き添えで死ぬ）、`3>&-`（**bats は fd 3 で結果を集める**ので握ったままだと bats が固まる）、**サーバを先に畳んでから `rm -rf`**（ディレクトリを消してもプロセスは死なない ── それがまさに 1 日生き残った理由）。**(3) 次の実行で拾う** ── プロセスグループごと SIGKILL されると見張りも死ぬ。**「終わったときに片付ける」では原理的に届かない**ので、`setup_suite`（bats が自動で読む）と `make reap` から `test/reap-tmux` を呼ぶ。判定はサンドボックスの `owner.pid` で、**生きている持ち主のものには絶対に触らない**（同じマシンで複数セッションが同時にテストを回すのは日常）。pid の使い回しで「死んでいるのに生きて見える」ことはあるが見送られて次回に回るだけで、**逆は起きない**。報告は **fd 3 に書く**（bats は setup_suite の stdout を握り潰すので、素直に printf すると前回の異常終了に誰も気づけない）。**`bats-run-*` は拾わない** ── 持ち主を判別する手掛かりが無く、数 KB で macOS が数日で掃除する（プロセスが残る tmux サーバとは性質が違う）。**(4) 素の `tmux` を禁じる** ── 調査中に `fake-claude.bats` が**ヘルパを通さず既定のソケット＝利用者が実際に開いているサーバ**にセッションを立てているのを見つけた。`cc/` 接頭辞が無いので `ccs ls` にも出ない。専用サーバに寄せ、`make lint` が `test/**/*.bats` の素の `tmux` を落とすようにした（`[[ ]]` の検査と同じで、**破っても緑になる**種類の規約）。**新しいテストに歯があることを毎回実測した** ── `-L` に戻すと `tmux-socket.bats` の 3 件が落ち、素の `tmux` に戻すと lint が落ちる。3 層は通しで実測: 正常終了→取り残し 0、Ctrl-C→完走後 0、**プロセスグループごと SIGKILL→サーバ 1 とサンドボックス 1 が残り、次の実行の `setup_suite` が回収**。導入時に実際の残骸（亡霊が使っていた `ccs-test.AvmqJo` を含む 7 個のサンドボックスと 3,950 個のソケット）を回収した。**CI で 1 度落ちた** ── `date -v-2H` は BSD 系だけの書き方で、GNU date は `-v` を知らない（AGENTS.md が名指しで禁じている「手元でだけ緑」の形）。固定の過去時刻に置き換えた。テスト 17 件、合計 401 件（unit 143 + integration 258） |
| 2026-08-26 | W3 `ccs gc` が worktree を片付けるようになった（[ADR-0003](docs/adr/0003-worktree-under-repo.md) 決定 7）。4 分類は **消す候補（clean + merge 済み + push 済み）/ 未 merge・未 push / dirty / prunable**。**安全性の本体は `git worktree remove` → `git branch -d` を force 無しで直列に通すこと**で、ccs 側の判定はその予告でしかない ── 実測: `worktree remove` は**追跡外ファイルだけでも拒む**、clean なら未 merge でも消すが**ブランチ ref が残るのでコミットは失われない**、`branch -d` は未 merge を拒む（**コミットを守るのはこの層**）、**worktree が生きている間は `branch -d` が拒む**ので `remove` → `branch -d` の順序は必然。この 2 つを素で通す限り「未 push かつ未マージ」は自動では消えない。**上流が無いブランチは未 push 側に倒す** ── git は上流が無いと `rev-list @{u}..HEAD` が fatal で落ちるので、空文字と `0` を区別しないと「push 済み」に誤判定する。merge 判定は `branch --merged HEAD`。**当初これを「`git branch -d` と同じ物差し」と書いたのは誤り**（2026-08-26 に訂正） ── `branch -d` は「HEAD に merge 済み **または上流に merge 済み**」で通す（実測: push 済みのブランチは main に未 merge でも警告付きで消える）ので、**あちらにとっては「push 済み」だけで十分**。gc は main に入ったことまで求めるぶん**意図的に厳しい**。厳しい側に倒れているので、消す候補に出したものが実行時に拒まれることは無い。逆は害になるが、こちらは「消せるものを報告に留める」だけ。dirty を merge 判定より先に見るのは、そもそも `remove` が拒むから。**列挙はディレクトリを走査せず `git worktree list --porcelain` に訊く** ── `prunable`（ディレクトリだけ消えた状態）は glob では原理的に拾えない。安い門番として `.git/worktrees` の有無を先に見る（`ghq list` は数百件になりうる）。**稼働中の判定でレジストリの slug 引きを踏んで 1 件落ちた** ── `live_cwd_for <slug>` は `"tmux":"cc/<slug>:"` で引き当てるので、**改名されたセッションが「居ない」ことになり、稼働中の worktree が消す候補に落ちた**（I1 の穴。テストが捕まえた）。作業枠なら作り直されるだけだが worktree は人のブランチごと消えかねないので、ここだけ先に名前への依存を切り、**レジストリを直接舐めて生きている claude の cwd を集める**形にした（ccs 以外の claude も数える）。`ccs kill` は worktree を消さず、片付け先を 1 行案内する（畳んだ時点がそのパスを引ける最後の機会）。ADR-0003 の未決だった **`ccs kill --prune-worktree` は「用意しない」で閉じた** ── 消してよいかは merge 済み・push 済みで決まり、それが真になるのは PR のマージ後で、畳むのはそれより前。オプションはほぼ必ず空振りする。テスト 23 件、合計 384 件（unit 143 + integration 241）。**手元で 1 周確認済み** ── ghq 配下 168 リポジトリのうち `.git/worktrees` を持つのは 8 件で、そのすべてが `.claude/worktrees/` か兄弟配置か `/tmp` にあり、`<repo>/.worktrees/` 配下は ccs のものだけだった（走査は 0.7 秒）。`close-19`（未 push 1 コミット）が「未 merge・未 push 1」として報告側に落ち、稼働中の自セッションの worktree は候補に出ない |
| 2026-08-26 | W2 worktree の置き場所を `<repo>/.worktrees/<branch-slug>` に移し、`CCS_WORKTREE_ROOT` を削除した（[ADR-0003](docs/adr/0003-worktree-under-repo.md) 決定 1・2・4・5・6）。**破壊的変更。移行はしない。** 親の作業ツリーは `.git/info/exclude` の `/.worktrees/` で塞ぐ ── 塞がないと `git status` に出るだけでなく、**`git add -A` が nested worktree を gitlink としてコミットに載せる**（実測）。`.gitignore` ではなく exclude なのは、あちらが追跡対象で所有していないリポジトリに差分を作るため。exclude は `--git-common-dir` 経由で**全 worktree に共有される**ので、1 回書けば入れ子も塞がる（実測）。ドット始まりは飾りではなく、gitignore を読まない道具（`go build ./...`・pytest・analyzer・`rg` の hidden 既定）への独立した防御。worktree を repo 部に指されたら**拒まず本体へ読み替える**（決定 4）── 拒否は人が打ち直す前提で入れ子を作れる形が残るが、読み替えれば原理的に生まれず slug も自動で正しくなる。`feat-foo` と `feat/foo` が同じディレクトリに落ちる穴は、既存の `HEAD` を見て落とす形で塞いだ（git は `feat` と `feat/foo` の共存は禁じるが、この 2 つは禁じない。実測）。`.worktrees` を既に追跡しているリポジトリでは、隠さず落ちる。`restore` の候補 3 は舐める根が無くなったので、`ghq list` の各リポジトリに `.worktrees/` があるかを見て回る形にした。**テストの差し替え点が 1 つ減った** — 置き場所がリポジトリのパスから導出されるので、`CCS_WORKTREE_ROOT` を逃がす必要が無い（`test_helper.bash` と AGENTS.md の行を削除）。テスト 9 件、合計 361 件（unit 143 + integration 218） |
| 2026-08-26 | W1 worktree の素性判定を git に移した（[ADR-0003](docs/adr/0003-worktree-under-repo.md) 決定 3）。**同じディレクトリが打ち方で 2 つの slug に割れていた** — `ccs resolve <worktree の実パス>` は末尾要素を返し、`ccs resolve <repo>@<branch>` は `<repo>@<branch>` を返す。同じ作業ツリーに 2 本の claude が立つ、冪等性 (S5) の穴。信頼の判定も解決時に立てた変数に依存していたので、実パスで指すと 30 秒の信頼確認に落ちた（ADR-0002 表 #9）。入れ子の拒否は `CCS_WORKTREE_ROOT` の前方一致で当てていたので、**根の外に手で切った worktree を素通し**していた。3 つとも `--git-dir` と `--git-common-dir` の食い違いで判定する形に置き換えた。**`git worktree list` で判定してはいけない** — submodule の中から呼ぶと、あれは作業ツリーではなく **git-dir**（`.git/modules/<名前>`）を先頭に並べるので、`--show-toplevel` と比べると worktree に見える（実測。一度この形で書いて submodule を誤検出した）。安い門番として先に `.git` がファイルかを見る（`restore` は ghq 配下を全部舐めるので、大多数を git を起こさずに落とせる）。分離 HEAD と bare の worktree は素性として扱わない。**`restore` の候補 3 はあえて触っていない** — slug を git から引くと、worktree の中でブランチを切り替えたときに tmux にある名前と食い違い、**動いているセッションを「消えた」として候補に出す**（実測）。占有を名前で当てているのが根本原因なので、I1（照合を cwd へ）の仕事。置き場所は変えていない（W2）。テスト 12 件、合計 352 件（unit 143 + integration 209）。**当初この行に「合計 209 件」と書いたのは誤り** — あれは integration だけの数で、unit を落としていた |
| 2026-08-25 | **P1 / P2 を落とし、[#19](https://github.com/ken-ty/ccs/issues/19) を閉じた。**あの 2 つは「端末そのものには届かないので、**せめて中身をテキストで覗く**」という代替案だった（`ccs peek` = アプリ → ハブ → `tmux capture-pane` → 会話にテキスト）。**諦めていた「端末そのものに届く経路」が見つかったので不要になった** ── Ken 所有の tailnet に profile を切り替え、GUI 版の Tailscale をやめて Homebrew の `tailscaled` にすると Tailscale SSH が有効になり、iPhone の Termius から `ccs attach` が通る（実地で確認。[docs/mobile-terminal.md](docs/mobile-terminal.md) §10）。**`capture-pane` の写しは本物の端末の劣化版**なので、二重に持つ理由が消えた。**制約そのものは消えていない** ── Claude アプリからは今も端末が見えない（why.md §4）。別の経路で埋めたので issue として閉じる。合わせて `ccs attach` に引数なしの選択（#43）を入れた ── スマホでは slug の入力が高くつくため。**「`send-keys` によるプロンプト注入は採らない」（design.md §3）は落ちない** ── あれは上の「確定している設計方針」表が持っているもので、P2 の備考に間借りしていたわけではない。mobile-terminal.md §1.1 は、それを今回の用途に当てはめて「Claude の TUI に文章を送るのではなく、別 window を切ってシェルに送る」と具体化している |
| 2026-08-22 | `restore` の 2 件が Linux の CI でだけ落ちていたのを直した。**原因は fake claude が SIGHUP を捕まえていなかったこと。** tmux は `kill-session` / `kill-pane` でペインのプロセスに SIGHUP を送るが、スタブの trap は EXIT / TERM / INT だけだった。**EXIT の trap が走るかどうかはシェル実装に依る** ── macOS の `/bin/sh` は bash 3.2 で走り、Linux の `/bin/sh` は dash で走らない。走らない側ではレジストリの残骸が消えず、立て直したセッションと 2 件並ぶ。どちらを掴むかは `*.json` の glob 順（＝ pid の文字列順）任せなので、**マシンによって結果が変わる**。スタブに HUP の trap を足し、テストも 1 件足した。合わせて土台を 2 つ固めた ── `ccs_kill_claude_of` は死んだ pid のファイルを飛ばす（`ccs` 側の `registry_is_live` と同じ判定。残骸を掴むと kill が空振りしてファイルも消えず時間切れになる）、`_wipe_session` はレジストリから消えるまで待つ（tmux セッションが消えてもプロセスの片付けには間がある）。テスト 336 件（unit 137 / integration 199） |
| 2026-08-22 | **手元の 207 件のアサーションが空振りしていた**のを塞ぎ、それが隠していた実装の穴を直した。発端は「Linux の CI でだけ 1 件落ちる」で、原因はテストでも環境差でもなく **macOS 標準の bash 3.2** ── テストの途中に置いた `[[ ]]` は、失敗しても errexit で落ちない（実測: `false` / `[ ]` / 関数の戻り値はどれも落ちるのに `[[ ]]` だけ素通りする。bash 4 で直っている）。つまり `make test` は手元で緑になりながら、`[[ ]]` を 1 つも検証していなかった。全 17 ファイル 207 件に `|| return 1` を付け、`make lint` に素の `[[ ]]` を落とす検査を足した（**書式の好みではなく、空振りするテストの禁止**）。有効にして走らせたら落ちたのは 1 件だけで、それが本命 ── `ccs new <repo>@<branch>` は、**ブランチが既にあって他所の worktree で開かれているとき、git の文言だけ出して次の一手を出していなかった**。git は 1 ブランチ 1 worktree しか許さないので「他所で開かれている」はまさにこの経路で起きるのに、案内は新規ブランチ側（`-b`）にしか無かった。失敗の出口を `worktree_add_failed` にまとめて両方から呼ぶ形にした |
| 2026-08-22 | V4 ドキュメントを GitHub Pages へ（<https://ken-ty.github.io/ccs/>）。**リポジトリを public にしたので、Artifacts に留めていた理由が消えた** ── 以前の判断は「private + Free プランでは Pages を publish できず、できてもサイトは公開される」だったが、前者は前提ごと無くなり、後者は公開が目的になった。**Artifacts は URL で読めない**（zip を落として展開して index.html を開くしかない）ので、他人に渡せるものになっていなかったのが実務上の理由。`docs.yml` は `upload-artifact` を `upload-pages-artifact` + `deploy-pages` に替え、`concurrency` を **`cancel-in-progress: false` / group は `pages` 固定**にした（デプロイするワークフローを途中で切ると、公開中のサイトが中途半端な状態で止まりうる。group を ref にしないのはデプロイ先が 1 つしか無いため）。`mkdocs.yml` に `site_url` と `repo_url` を足した |
| 2026-08-22 | R1 `ccs restore`（v2 候補だった V1 を、実機の要求で前倒し）。**PC の再起動で hub 以外の 8 本が offline のまま戻らなかった**のが出発点。会話は `~/.claude/projects/**.jsonl` に残るので、同じ場所で `claude --resume <uuid>` を立てれば戻る ── それを候補ごと見つけてまとめてやる。**列挙の根拠を durable な場所に置いた** ── レジストリ（`~/.claude/sessions/<pid>.json`）は**再起動を跨いで残っていなかった**（実測。pid キーなので残っても再利用と衝突する）ので、`ccs` 自身が場所を決めているところ（止まったペイン / `~/.cc-scratch/<n>` / `~/.cc-worktrees/<repo>/<branch>`）だけを舐める。**ghq 配下は列挙しない** — そこの会話ログはデスクトップアプリや VS Code のものと混ざっている。**エンコード規則は前向きにしか使えない**（`/` も `.` も `_` も `-` に潰れるので逆算不能。`claude --resume` の一覧も対話ピッカーしか無い）ので、当たった会話ログの中の `cwd` で答え合わせをして、食い違ったら飛ばす。**`ccs hub up` には混ぜない** — 5 分おきに走るので、人が畳んだものが生き返る（`ccs` は「畳んだ」と「落ちた」を durable な情報から区別できない）。代わりに hub を実際に立て直したときだけ 1 行案内する。**`-n` を渡さない**のが `ccs new` との唯一の違い ── 被せると会話に付いた名前が消える。既定は dry-run（戻した会話はその場で走り出すことがあり、止める手段は `ccs` に無い）。ついでに `pane_start_command` から uuid を拾う正規表現が `--resume` を見ていなかったのを直した（一度戻したセッションの復帰路が二度目から消えていた）。本物の claude で 1 周検証済み（同じ sessionId・会話の継続・名前の保持・`bridgeSessionId` の復帰）。テスト 31 件 |
| 2026-08-22 | H7 のうち **launchd の経路を実測**し、そこで見つかった不具合を直した。`ccs hub agent` が出す plist は `-lc` でログインシェルを起こすだけだったので、**PATH を `~/.zshrc` で足している環境（Homebrew の既定の入れ方）では tmux も claude も見つからず、`ccs hub up` が依存不足で即死していた** ── `agent.log` に同じ依存不足が積まれるだけで、hub は永久に立たない。launchd が渡すのは `/usr/bin:/bin:/usr/sbin:/sbin` 相当で、`-lc` で読まれるのは `/etc/zprofile` と `~/.zprofile` まで（`~/.zshrc` は対話シェル専用）。**生成時に解決できた依存の在処をユニットに焼き込む**形にした（launchd は `EnvironmentVariables`、systemd は `Environment=PATH=`）。焼き込みなので**ユニットは生成した環境に紐づく** — Homebrew を動かしたら出し直す、と docs に明記した。直したうえで `tmux kill-server` → `launchctl bootstrap` を通し、**launchd から起動した ccs が手元と同じ tmux サーバ（`/private/tmp/tmux-501/default`）に入る**ことを確認 ── ここが崩れると設計ごと組み直しだった項目が閉じた。残る H7 はアプリ側の見え方だけ。テスト 302 件 |
| 2026-08-20 | C1 `ccs new <repo>@<branch>`。**1 リポジトリに 2 本目のセッションが立たない**のが ccb の唯一の構造的ブロッカーだった（slug がリポジトリ名なので `ccs new x01` は冪等に 1 本目を返す）。置き場所を決める過程で **`ghq list` が `<repo>.worktrees/<name>` を独立したリポジトリとして列挙する**ことが分かったので、ghq root の外（`CCS_WORKTREE_ROOT`、既定 `~/.cc-worktrees`）に置いた — 中に置くと `resolve_as_repo` の末尾一致が worktree に当たり、`ccs new <branch-slug>` が worktree を掴む。**ghq 配下のリポジトリの worktree は自動で信頼する** — 中身は人が意図して clone したものそのもので、これが無いとタスクごとに 30 秒の信頼確認で固まる。**作るのは `ccs new` だけで `ccs resolve` は作らない**（枠と違い repo と branch から一意に決まるので、解決に副作用が要らない）。`/tmp`→`/private/tmp` の正規化漏れを実装とテストの両方で踏んだ（design.md §6 と同じ形）。テスト 26 件、合計 299 件 |
| 2026-08-19 | H1〜H6 `ccs hub`（up / status / restart / down / attach / agent）と設定層。**スマホから使うと最初に壊れるのがハブ自身**だったので、落ちても戻る形にした ── `up` は冪等で、`bridgeSessionId` が付くまで待って初めて「立った」と見なす（RC が付かないセッションはアプリに出ない＝実質死。実測で null のまま残る個体があった）。**認証切れと再起動の暴走は自動で直さず止まる**（`/login` は対話が要る／無限再起動は課金とレート制限に直結する）。合わせて、既定値が作者の環境に固定されていた問題を設定層で解いた ── `hub` はリポジトリ名としてありふれているので `CCS_HUB_SLUG` で変えられ、RC を使わない環境は `CCS_REMOTE_CONTROL=off`、自動起動は `CCS_HUB_AUTOSTART` で on/login/off。設定ファイルは **source しない**（設定と実行の境界を保つため）。`ccs kill` は hub を `--force` でも拒否し、自セッションの kill には `--force` が要る。macOS の bash 3.2 + bats で日本語のテスト名が化ける問題も `LC_ALL=C` で塞いだ（他人の環境で `make test` が 1 件も走らない状態だった）。テスト 273 件 |
| 2026-08-19 | `ccs` のセッションは **Claude アプリから端末の画面が見えない**制約を文書化（[#19](https://github.com/ken-ty/ccs/issues/19)）。レジストリ全件で `entrypoint` が `claude-desktop` / `claude-vscode`（`tmux` 無し）と `cli`（`tmux` 有り）に二分されることを確認し、**pty を持つ主体が違う**という推定を書いた（アプリ側の実装は未確認なので未検証と明記）。`ccs` 固有ではなく手で立てた tmux セッションも同じだが、**ccs を使うと全セッションがこうなる**ので ccs の制約として現れる。`ccs attach` が同一マシン限定であることを README と docs/index.md にも書いた。**why.md §4 で tmux の利点として挙げた「デスクトップアプリからの独立」が、ここでは代償として効いている** |
| 2026-08-18 | `docs/why.md` を追加。**README が「なぜ薄いか」だけを答えていて「なぜ要るか」が書かれていなかった** — 委譲表だけを読むと「残り 4 つは手順書で足りる」が正しい推論になってしまう。素のセッションで新しいセッションが立たない理由を実測で残した（pty が無く対話モードが `--print` に落ちて即死する / `SendMessage` は既存宛のみ / `Agent` は独立セッションではない）。禁止ではないことも実測で示した（入れ子の `claude -p` は通る）—— ここを取り違えると「外から SSH で入る」別の問題を解きに行くことになる。手で tmux を叩いた比較も実走し、trust で固まること・`cc/` を落とすと `ccs ls` から漏れることを確認。tmux を選ぶ理由に attach・長時間稼働・デスクトップアプリからの独立を明記（切断頻度の比較は未検証と明示） |
| 2026-08-18 | V3 使い捨て枠の自動信頼（[#6](https://github.com/ken-ty/ccs/issues/6) の回答）と `--tmp` の導入。**実機で 2 本目の枠が立たないことが分かって決まった** — 枠 1 本ごとに承認が要り、`ccs new tmp` が信頼ダイアログで固まって 30 秒後に失敗する。枠は `ccs` 自身が作った空のディレクトリなので、確認が守る「知らないコード」がそこに無い。**空であることは毎回確かめる**ので、中身のある枠を実パスで指したときは従来どおり自動承認しない。合わせて、予約語 `tmp` がリポジトリ名と名前空間を共有している問題を `--tmp` で解消（`tmp` も残すが、ghq に同名があれば IceCubesApp と同じく候補を出して止まる）。テスト 196 件 |
| 2026-08-17 | 設計調査を実施し、自作範囲を 4 つに確定（`545e370`） |
| 2026-08-17 | S1 `bin/ccs` の骨格。終了コードを対外契約として固定（0/1/2/3/4）し、「未知(2)」と「未実装(3)」を区別。テストのサンドボックス化は `test_helper.bash` に集約（1 箇所でも漏れると実環境を壊すため）。CI に「本物の claude が存在しないこと」の番人を置いた |
| 2026-08-17 | S13 `cross-session-hub` スキルを改訂（スキルの store は **private リポジトリ**）。旧文面の 2 点が実測と食い違っていた — `ccd_session_mgmt` はデスクトップ限定で VS Code も tmux も見えないこと、「新規セッションは開けない」が `ccs` で成り立たなくなったこと。読み取り専用の原則は変えず、むしろ「立てた相手も別セッションであることに変わりはない」を明示した。**これで v1 の全項目が完了** |
| 2026-08-17 | S12 `docs/hands-on.md`。**全 10 手順を本物の claude で上から順に実走してから書いた。** 信頼確認の 30 秒待ち、`/exit` 後にペインが zsh へ戻ること、2 回目は確認が出ないこと、stopped の枠を `ccs new tmp` が掴まないこと、をすべて実機で確認。手順 4（ハブの `ListAgents` に現れて `SendMessage` で頼める）を「ここが本命」と明示し、他が引っかかっても使い勝手の話だと分けた。検証後に tmux・作業枠とも撤去済み（trust に 1 行だけ残るのは手順書に明記） |
| 2026-08-17 | S11 `docs/tutorial.md`。**画像 10 枚すべて本物の claude で実際に動かした出力**（作り物の画面は 1 枚も無い）。撮影の過程で ListAgents に 2 本とも現れることを再確認し、`~/.claude.json` の trust が書き換わっていないことも差分で確認した（x01・catan とも既に信頼済みだったため）。ついでに README を v1 の状態に更新し、install 手順（symlink 1 行）を書いた |
| 2026-08-17 | S10 ドキュメント基盤。MkDocs Material を `uvx` 経由で動かし（グローバルに入れると手元と CI で版がずれる）、依存はピン留め。`scripts/termshot.py` は**実行結果のキャプチャを SVG にする道具**で、標準ライブラリのみ・決定的（同じ入力なら同じ SVG なので差分がレビューできる）・light/dark 両対応・全角の幅を考慮。日本語見出しの anchor が既定 slugify で番号だけになる問題を pymdownx の unicode slugify で解消（GitHub 上のリンクと揃える）。Pages へは出さず Artifacts（private + Free プランでは publish できず、できてもサイトは公開されるため）。テスト 13 件 |
| 2026-08-17 | S9 `ccs gc`。**既定は dry-run**（`ghq rm --dry-run` と同じ作法。ハブのエージェントが打つ前提だと確認なしで消える設計は割に合わない）。**中身の残っている作業枠は報告するだけで絶対に消さない** — そこにあるのは利用者のファイル。空枠は `rm -rf` ではなく `rmdir` で消す（空だと思ったら空でなかったときに黙って消さないため）。畳む前に uuid を見せる — 畳むと `pane_start_command` ごと消えるので最後の機会。**これで v1 のコマンドが揃った**（テスト 20 件、合計 168 件） |
| 2026-08-17 | S8 `ccs attach` / `ccs kill`。attach は tmux の中にいれば `switch-client` に切り替える（入れ子の attach は tmux が拒むので、ハブが tmux 内で動いていると乗り込めなくなる）。kill は **作業中なら `--force` 無しでは畳まない** — 会話は transcript に残るが走っていたコマンドの途中経過は戻らない。畳む前に uuid と cwd を集めて復帰コマンドを出す（畳んだあとでは何も引けない）。`set -e` の下で `var=$(失敗する関数)` が即終了するバグをテストが捕まえた。テスト 21 件 |
| 2026-08-17 | S7 `ccs ls`（表 / `--json`）。**ROADMAP の前提が誤りだった** — `claude agents --json` は `tmux` フィールドを持たないので、あの出力を `cc/` で絞ることはできない。tmux 側を起点にし、中身はレジストリから引く形に変えた（design.md §2.1 に訂正を追記）。stopped でも `pane_start_command` から uuid を出す。`cc/` が付かない tmux セッションは巻き込まない。テスト 17 件 |
| 2026-08-17 | S6 trust の事前承認。自動承認は **ghq 配下だけ**に限り、作業枠と ghq 外は「確認が出る」と先に伝えて進む（黙って立てると 30 秒待たされて理由だけ分からない）。`~/.claude.json` は 100KB 級の実状態ファイルなので、書く前後で JSON の妥当性と projects の件数を確かめ、通ってから mv で置き換える。壊れた JSON は直そうとせず触らない。macOS の `/var`→`/private/var` で ghq root の前方一致が外れるバグを実装で踏み、**テスト 2 件が空振りで通っていた**ことも判明したので、対象自身が信頼済みになったかを見る形に直した。枠の自動承認の是非は [#6](https://github.com/ken-ty/ccs/issues/6) へ。テスト 14 件 |
| 2026-08-17 | S5 `ccs new` の冪等性。同じ slug は立て直さず既存を返す（二重に立てると同じ作業ツリーを 2 本の claude が触る）。**`ccs new tmp` だけは例外**で、立っている枠は掴まず次を取る — 作業中でも中身が空なことがあり、ディレクトリだけ見ると動いているセッションを返してしまう。ペインは生きているが claude が死んでいる場合は成功扱いにせず、`pane_start_command` から uuid を回収して `claude --resume` の導線を出す（v1 に resume が無いので、これが唯一の復帰路）。作業枠を実パスで指すと slug が `1` になるバグも直した。テスト 15 件 |
| 2026-08-17 | S4 `ccs new`。「立った」の判定を tmux ではなくレジストリで行う（ペインが生きていても claude が起動していないことがあるため）。登録されないときはセッションを消さずペインの中身を見せる — 何に詰まったかはそこにしか出ない。**本物の claude で実機確認済み**: 立った直後にハブの `ListAgents` に現れ、レジストリに `"tmux":"cc/agent-skills:@0.%0"` が入り、transcript のパス予測も一致した（ただしファイル自体は初回のやり取りまで作られない）。テスト 20 件 |
| 2026-08-17 | S3 `resolve_target` と `ccs resolve`。**打ち方に依らず同じ slug になる**ことを軸に据えた（`x01` / `ken-ty/x01` / `github.com/ken-ty/x01` が同一）── ここがブレると S5 の冪等性が成立しない。曖昧なときは勝手に選ばず候補を出して落とす。design.md の `tmp-<6桁>` は固定枠の決定と矛盾していたので `tmp-<枠番号>` に直した。テスト 27 件 |
| 2026-08-17 | S2 `test/fixtures/fake-claude`。実測した本物のレジストリ形式を再現し、`agents --json` と tmux フィールドまで持つ。異常系を作るつまみ（登録遅延・登録しない・自ら終了）を先に用意したので、S4 の待ちループとタイムアウトがテスト可能になった。**スタブ自身に 18 件のテストを付けた** — スタブが間違うと以降のテストは落ちずに「間違ったものを検証する」ため |
